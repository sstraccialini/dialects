# Word2Vec baseline

This folder contains the Word2Vec pipeline for the Language Technology Project.

## Run

From the repository root:

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r word2vec/requirements.txt
python word2vec/src/run_word2vec.py
