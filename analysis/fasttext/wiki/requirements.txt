# Dependencies for the Subword / FastText approach (Person 5).
# The repo-level requirements.txt already covers pandas and numpy.
# Install with:  pip install -r subword_fasttext/requirements.txt

# FastText subword embeddings (gensim implementation)
gensim>=4.3.0

# BPE tokenization (Google SentencePiece)
sentencepiece>=0.1.99

# TF-IDF + distance + clustering (shared with baseline)
scikit-learn>=1.3.0
scipy>=1.10.0

# Plotting
matplotlib>=3.7.0
seaborn>=0.12.0
