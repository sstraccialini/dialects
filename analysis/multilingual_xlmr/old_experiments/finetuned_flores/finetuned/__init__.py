"""XLM-R fine-tuning module — MLM/TLM training routines and the 4-condition pipeline.

This package holds everything specific to fine-tuning XLM-R (training
hyperparameters, model checkpoint paths, MLM/TLM loops, training data
loaders for parallel pairs and dialect Wiki text). Anything shared with
the unadapted XLM-R baseline lives in `analysis.multilingual_xlmr.core`.
"""
